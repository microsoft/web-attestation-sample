{
  "name": "uwa-sample-issuer",
  "version": "0.0.1",
  "description": "Sample Issuer of User-centric Web Attestations",
  "scripts": {
    "build": "tsc",
    "setup-issuer": "ts-node --files src/setup.ts",
    "deploy-issuer": "ts-node --files src/issuer.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "MIT",
  "dependencies": {
    "commander": "^10.0.0",
    "express": "^4.18.2",
    "express-rate-limit": "^6.7.0",
    "uprove-node-reference": "github:microsoft/uprove-node-reference"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "ts-node": "^10.9.1"
  },
  "type": "module"
}
